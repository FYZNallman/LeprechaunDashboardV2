<script lang="ts">
	export default {
		// props: {
		// 	name: {
		// 		type: String,
		// 		default: 'LiveChart',
		// 	},
		// },

    // methods: {
		// 	// // Use the addToCart function like this:
		// 	// // Eg-1: addToCart('pizza', 10)
		// 	// // Eg-2: (event) => addToCart('pizza', 10, event)
		// 	addToCart(item, quantity /*, event*/) {
		// 		this.$emit('addToCart', { item, quantity })
		// 	},
		// },
	}
</script>

<template>
	<div style="min-height: 100px; display: flex; align-items: center; justify-content: center; font-weight: bold">
		<span style="color: #0198cd">LiveChart.vue (Vue 3.4)</span>
	</div>
</template>

<style scoped></style>

